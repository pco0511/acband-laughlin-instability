[project]
name = "acband-laughlin-instability"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "einops>=0.8.1",
    "numpy>=2.3.4",
]

[tool.uv.workspace]
members = [
    "figs",
]
